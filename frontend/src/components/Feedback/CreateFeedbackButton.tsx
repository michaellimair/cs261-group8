import { Button } from '@chakra-ui/react';
import { FC } from 'react';

const CreateFeedbackButton: FC = () => (
  <Button colorScheme="green">
    Create Feedback
  </Button>
);

export default CreateFeedbackButton;
